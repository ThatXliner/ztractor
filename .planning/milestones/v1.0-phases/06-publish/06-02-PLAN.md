---
phase: 06-publish
plan: 02
type: execute
wave: 2
depends_on:
  - "06-01"
files_modified: []
autonomous: true
requirements:
  - PUB-01
  - PUB-02

must_haves:
  truths:
    - "Both packages build successfully with current source"
    - "All tests pass after version bump"
    - "dist/ directories contain fresh build artifacts"
  artifacts:
    - path: "packages/core/dist/index.js"
      provides: "Core package built output"
    - path: "packages/core/dist/index.d.ts"
      provides: "Core package type definitions"
    - path: "packages/node/dist/index.js"
      provides: "Node package built output"
    - path: "packages/node/dist/index.d.ts"
      provides: "Node package type definitions"
  key_links:
    - from: "packages/node/dist/index.js"
      to: "packages/core/dist/index.js"
      via: "ztractor import"
      pattern: "from.*ztractor"
---

<objective>
Build both packages fresh and verify everything is publish-ready: tests pass, dist is current, package.json exports resolve correctly.

Purpose: Final verification that both packages are in a shippable state before the user runs npm publish.
Output: Fresh dist/ builds for both packages, all tests green, publish checklist.
</objective>

<execution_context>
@/Users/bryanhu/Developer/Pending/ztractor/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bryanhu/Developer/Pending/ztractor/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-publish/06-CONTEXT.md
@.planning/phases/06-publish/06-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build both packages and run all tests</name>
  <files></files>
  <read_first>
    - packages/core/package.json
    - packages/node/package.json
  </read_first>
  <action>
Run the following commands in sequence from the repo root:

1. Build all packages: `bun run build`
   - This builds core first (bundle-translate, bundle-translators, bunup) then node (bunup)

2. Verify dist artifacts exist:
   - `ls packages/core/dist/index.js packages/core/dist/index.d.ts`
   - `ls packages/node/dist/index.js packages/node/dist/index.d.ts`

3. Run all tests: `bun test`
   - All existing tests must pass (173+ tests)

4. Verify package.json exports fields resolve:
   - Core: `node -e "import('file://' + process.cwd() + '/packages/core/dist/index.js').then(m => console.log('core OK, exports:', Object.keys(m).slice(0,5).join(', ')))"`
   - Node: `node -e "import('file://' + process.cwd() + '/packages/node/dist/index.js').then(m => console.log('node OK, exports:', Object.keys(m).slice(0,5).join(', ')))"`

5. Dry-run publish check (do NOT actually publish):
   - `cd packages/core && npm pack --dry-run 2>&1 | head -30`
   - `cd packages/node && npm pack --dry-run 2>&1 | head -30`
   - Verify both list only dist/ files and package.json

If any step fails, investigate and fix. The build must succeed cleanly.
  </action>
  <verify>
    <automated>cd /Users/bryanhu/Developer/Pending/ztractor && bun run build && bun test && ls packages/core/dist/index.js packages/node/dist/index.js && echo "ALL PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `bun run build` exits 0
    - `bun test` exits 0 with all tests passing
    - packages/core/dist/index.js exists
    - packages/core/dist/index.d.ts exists
    - packages/node/dist/index.js exists
    - packages/node/dist/index.d.ts exists
    - `npm pack --dry-run` for both packages shows only dist/ files
  </acceptance_criteria>
  <done>Both packages build and test successfully, dist artifacts are fresh, npm pack dry-run confirms correct file list</done>
</task>

<task type="auto">
  <name>Task 2: Write publish instructions in plan summary</name>
  <files></files>
  <read_first>
    - packages/core/package.json
    - packages/node/package.json
  </read_first>
  <action>
When writing the 06-02-SUMMARY.md, include a "Publish Checklist" section with these exact instructions for the user:

```
## Publish Checklist

Prerequisites:
- [ ] Run `npm login` (if not already logged in)
- [ ] Verify you're on the correct npm account: `npm whoami`

Publish core package first (node depends on it):
- [ ] `cd packages/core && npm publish`
- [ ] Verify: `npm view ztractor version` should show `1.0.0`

Then publish node package:
- [ ] `cd packages/node && npm publish`
- [ ] Verify: `npm view ztractor-node version` should show `1.0.0`

Post-publish verification:
- [ ] `mkdir /tmp/ztractor-test && cd /tmp/ztractor-test && npm init -y && npm install ztractor-node`
- [ ] Create test.mjs with the quick-start example from the README
- [ ] `node test.mjs` should extract metadata from the arXiv URL
```

This is informational only — do NOT execute any publish commands.
  </action>
  <verify>
    <automated>echo "PASS — instructions are informational, included in summary"</automated>
  </verify>
  <acceptance_criteria>
    - 06-02-SUMMARY.md contains "Publish Checklist" section
    - Instructions mention `npm login`, `npm publish` for both packages, and post-publish verification
    - No `npm publish` or `bun publish` commands were actually executed
  </acceptance_criteria>
  <done>Publish checklist with step-by-step instructions is documented in the plan summary for user to follow</done>
</task>

</tasks>

<verification>
- `bun run build` succeeds from repo root
- `bun test` passes all tests
- Both dist/ directories have fresh .js and .d.ts files
- `npm pack --dry-run` for each package shows correct file list
- No `npm publish` was executed
</verification>

<success_criteria>
Both packages build cleanly, all tests pass, dist artifacts are fresh, and the user has a clear publish checklist to follow. The packages are in a publish-ready state.
</success_criteria>

<output>
After completion, create `.planning/phases/06-publish/06-02-SUMMARY.md`
</output>
