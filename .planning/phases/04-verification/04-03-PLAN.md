---
phase: 04-verification
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - bunfig.toml
autonomous: true
requirements:
  - VERIFY-02
gap_closure: true

must_haves:
  truths:
    - "`bun test packages/core/` from repo root exits 0 with 0 failures"
    - "Root-level bunfig.toml preload points to packages/core/tests/setup.ts"
  artifacts:
    - path: "bunfig.toml"
      provides: "Root-level Bun test configuration with preload"
      contains: "packages/core/tests/setup.ts"
  key_links:
    - from: "bunfig.toml"
      to: "packages/core/tests/setup.ts"
      via: "preload array"
      pattern: "packages/core/tests/setup\\.ts"
---

<objective>
Add a root-level bunfig.toml so that `bun test packages/core/` from the repo root picks up the test setup preload and exits 0.

Purpose: The PLAN acceptance criterion for VERIFY-02 requires `bun test packages/core/` from the repo root to exit 0. Currently 35 tests fail because Bun does not pick up `packages/core/bunfig.toml` when invoked with a path argument from a parent directory. The preload (`tests/setup.ts`) that provides `DOMParser` and `document` globals is skipped.

Output: `bunfig.toml` at repo root containing a `[test]` preload entry pointing to `packages/core/tests/setup.ts`.
</objective>

<execution_context>
@/Users/bryanhu/Developer/Pending/ztractor/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bryanhu/Developer/Pending/ztractor/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

Gap source: .planning/phases/04-verification/04-VERIFICATION.md (Gap 1)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create root-level bunfig.toml with test preload</name>
  <files>bunfig.toml</files>
  <read_first>
    - packages/core/bunfig.toml — existing preload config (preload = ["./tests/setup.ts"])
    - packages/core/tests/setup.ts — verify the file exists and is the correct entry point
  </read_first>
  <action>
Create `/Users/bryanhu/Developer/Pending/ztractor/bunfig.toml` with this exact content:

```toml
[test]
preload = ["./packages/core/tests/setup.ts"]
```

This mirrors the existing `packages/core/bunfig.toml` but uses a repo-root-relative path so Bun picks it up when invoked as `bun test packages/core/` from the monorepo root.

Do NOT modify `packages/core/bunfig.toml` — it must remain unchanged for `cd packages/core && bun test` to continue working.

The root bunfig.toml only needs the `[test]` section. No other configuration needed.
  </action>
  <verify>
    <automated>cd /Users/bryanhu/Developer/Pending/ztractor && bun test packages/core/ 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `bunfig.toml` exists at repo root
    - File contains exactly: `[test]` and `preload = ["./packages/core/tests/setup.ts"]`
    - `bun test packages/core/` from repo root exits 0 with 0 failures (212+ pass, 5 skip)
    - `cd packages/core && bun test` still exits 0 (unchanged behavior)
    - grep check: `grep "packages/core/tests/setup.ts" bunfig.toml` returns a match
  </acceptance_criteria>
  <done>Root-level bunfig.toml created; `bun test packages/core/` from repo root exits 0 with all tests passing.</done>
</task>

</tasks>

<verification>
Run from repo root:

```bash
bun test packages/core/ 2>&1 | grep -E "^(pass|fail|Tests run|✓|✗)" | tail -10
```

Expected: 0 failures. All previously-failing 35 tests should now pass because the preload supplies `DOMParser` and `document` globals.

Also confirm the package-level invocation still works:
```bash
cd packages/core && bun test 2>&1 | tail -3
```
</verification>

<success_criteria>
- `bun test packages/core/` from repo root exits 0
- 0 test failures (previously 35)
- bunfig.toml at repo root contains correct preload path
- `cd packages/core && bun test` unchanged — still passes
</success_criteria>

<output>
After completion, create `.planning/phases/04-verification/04-03-SUMMARY.md`
</output>
