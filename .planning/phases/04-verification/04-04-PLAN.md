---
phase: 04-verification
plan: 04
type: execute
wave: 2
depends_on:
  - 04-03
files_modified: []
autonomous: false
requirements:
  - VERIFY-01
gap_closure: true

must_haves:
  truths:
    - "Wikipedia, arXiv, reddit, and NPR translator tests pass against live URLs"
    - "All 6 TRANSLATOR_COMPAT tests pass (2 harness tests + 4 live translator tests)"
  artifacts:
    - path: "packages/core/tests/zotero-compat.test.ts"
      provides: "TRANSLATOR_COMPAT-gated live tests for 4 real-world translators"
      contains: "test.skipIf(!process.env.TRANSLATOR_COMPAT)"
  key_links:
    - from: "packages/core/tests/zotero-compat.test.ts"
      to: "live URLs (Wikipedia, arXiv, reddit, NPR)"
      via: "extractMetadata() with TRANSLATOR_COMPAT=1"
      pattern: "TRANSLATOR_COMPAT"
---

<objective>
Human verification of the TRANSLATOR_COMPAT live translator tests. Confirm that all 6 tests pass against live URLs.

Purpose: VERIFY-01 requires representative real-world translators (Wikipedia, arXiv, reddit, NPR) to pass their Zotero test cases. The tests exist and are correctly wired but cannot be verified without live network access. This checkpoint closes the gap with a human-run verification.

Output: Human confirmation that all 6 TRANSLATOR_COMPAT tests pass.
</objective>

<execution_context>
@/Users/bryanhu/Developer/Pending/ztractor/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bryanhu/Developer/Pending/ztractor/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

Gap source: .planning/phases/04-verification/04-VERIFICATION.md (Gap 2)
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Run TRANSLATOR_COMPAT live tests and confirm results</name>
  <read_first>
    - packages/core/tests/zotero-compat.test.ts — the 6 TRANSLATOR_COMPAT-gated tests
  </read_first>
  <what-built>
    TRANSLATOR_COMPAT-gated live tests exist in packages/core/tests/zotero-compat.test.ts.
    The 6 tests are:
    1. "harness can extract test cases from a real translator" (DOI, no TRANSLATOR_COMPAT needed)
    2. "harness can run a single translator test" (DOI, no TRANSLATOR_COMPAT needed)
    3. "Wikipedia: single article passes Zotero test case"
    4. "arXiv: single paper passes Zotero test case"
    5. "reddit: forum post passes Zotero test case"
    6. "news site: article passes Zotero test case" (NPR)

    Tests 3-6 require TRANSLATOR_COMPAT=1 and live network access to run.
  </what-built>
  <how-to-verify>
    From the packages/core directory, run:

    ```
    cd packages/core
    TRANSLATOR_COMPAT=1 bun test tests/zotero-compat.test.ts
    ```

    Expected output: All 6 tests pass with 0 failures.

    If any test fails, note which translator failed and what the actual vs expected output was.

    Note: These tests make live HTTP requests to Wikipedia, arXiv (arxiv.org), reddit.com, and NPR (npr.org). Ensure you have internet access before running.
  </how-to-verify>
  <acceptance_criteria>
    - All 6 tests pass (0 failures)
    - The 4 TRANSLATOR_COMPAT-gated tests each show "pass" in the output
    - No network errors or timeouts
  </acceptance_criteria>
  <resume-signal>
    Type "approved" if all 6 tests pass.
    Or describe which test(s) failed and the error output so issues can be diagnosed.
  </resume-signal>
</task>

</tasks>

<verification>
Human confirmation that `TRANSLATOR_COMPAT=1 bun test tests/zotero-compat.test.ts` (from packages/core/) exits 0 with all 6 tests passing.
</verification>

<success_criteria>
- All 6 TRANSLATOR_COMPAT tests pass against live URLs
- Wikipedia, arXiv, reddit, and NPR translators each extract metadata matching their Zotero test cases
- Human has typed "approved" to confirm
</success_criteria>

<output>
After completion, create `.planning/phases/04-verification/04-04-SUMMARY.md`
</output>
