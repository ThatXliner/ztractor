---
phase: 06-publish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/README.md
  - packages/node/README.md
  - packages/node/package.json
autonomous: true
requirements:
  - PUB-01
  - PUB-02
  - PUB-03

must_haves:
  truths:
    - "Core README shows arXiv quick-start with realistic output"
    - "Node README shows arXiv quick-start with realistic output and XPath caveat"
    - "ztractor-node version is 1.0.0"
    - "ztractor-node depends on ztractor ^1.0.0 (not workspace:*)"
  artifacts:
    - path: "packages/core/README.md"
      provides: "Core package README with arXiv quick-start"
      contains: "arxiv.org/abs/2303.08774"
    - path: "packages/node/README.md"
      provides: "Node package README with arXiv quick-start and XPath caveat"
      contains: "arxiv.org/abs/2303.08774"
    - path: "packages/node/package.json"
      provides: "Node package.json with version 1.0.0 and real semver dep"
      contains: "\"version\": \"1.0.0\""
  key_links:
    - from: "packages/node/package.json"
      to: "packages/core/package.json"
      via: "ztractor dependency version"
      pattern: "\"ztractor\": \"\\^1\\.0\\.0\""
---

<objective>
Update both READMEs with stable arXiv quick-start examples and realistic output, bump ztractor-node to 1.0.0, and fix workspace dependency for npm publish.

Purpose: Prepare package metadata and documentation so both packages are publish-ready.
Output: Updated READMEs and package.json files ready for npm publish.
</objective>

<execution_context>
@/Users/bryanhu/Developer/Pending/ztractor/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bryanhu/Developer/Pending/ztractor/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-publish/06-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update core README with arXiv quick-start example</name>
  <files>packages/core/README.md</files>
  <read_first>
    - packages/core/README.md
    - packages/core/package.json
  </read_first>
  <action>
Rewrite the Quick Start section of packages/core/README.md to use the stable arXiv URL `https://arxiv.org/abs/2303.08774` (GPT-4 paper). Replace BOTH the Browser and Node.js examples.

The Browser example should be:

```typescript
import { extractMetadata } from 'ztractor';

const result = await extractMetadata({
  url: 'https://arxiv.org/abs/2303.08774',
});

if (result.success) {
  const item = result.items[0];
  console.log(item.itemType);  // "preprint"
  console.log(item.title);     // "GPT-4 Technical Report"
  console.log(item.creators);  // [{ firstName: "Josh", lastName: "Achiam", creatorType: "author" }, ...]
  console.log(item.date);      // "2024-03-04"
  console.log(item.DOI);       // "10.48550/arXiv.2303.08774"
}
```

Keep the Node.js sub-section short — just point to `ztractor-node` package with `bun add ztractor-node` and `import { extractMetadata } from 'ztractor-node'` with a note that the API is identical.

Keep all other sections (Features, API, Browser Compatibility, Limitations, License, Credits, Related Projects) unchanged.
  </action>
  <verify>
    <automated>grep -c "arxiv.org/abs/2303.08774" packages/core/README.md | grep -q "1" && grep -c "preprint" packages/core/README.md | grep -q "1" && grep -c "GPT-4 Technical Report" packages/core/README.md | grep -q "1" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - packages/core/README.md contains `https://arxiv.org/abs/2303.08774` in quick-start
    - Output shows `itemType`, `title`, `creators`, `date`, `DOI` fields with realistic values
    - Node.js section references `ztractor-node` package
    - No NYTimes example URL remains in the file
  </acceptance_criteria>
  <done>Core README quick-start uses arXiv URL with realistic output showing preprint metadata</done>
</task>

<task type="auto">
  <name>Task 2: Update node README with arXiv quick-start and XPath caveat</name>
  <files>packages/node/README.md</files>
  <read_first>
    - packages/node/README.md
    - packages/node/package.json
  </read_first>
  <action>
Rewrite the Quick Start section of packages/node/README.md to use the stable arXiv URL `https://arxiv.org/abs/2303.08774`.

The example should be:

```typescript
import { extractMetadata } from 'ztractor-node';

const result = await extractMetadata({
  url: 'https://arxiv.org/abs/2303.08774',
});

if (result.success) {
  const item = result.items[0];
  console.log(item.itemType);  // "preprint"
  console.log(item.title);     // "GPT-4 Technical Report"
  console.log(item.creators);  // [{ firstName: "Josh", lastName: "Achiam", creatorType: "author" }, ...]
  console.log(item.date);      // "2024-03-04"
  console.log(item.DOI);       // "10.48550/arXiv.2303.08774"
}
```

In the Technical Details > DOM Implementation section, add a note at the end:
"**Note:** XPath support uses an xmldom bridge (XPath 1.0). XPath 2.0 is not supported."

Keep the Node.js-Specific Example section but update its URL to use a more realistic example (e.g., `https://arxiv.org/abs/2303.08774` with saving to file).

Keep all other sections (Why Use ztractor-node, API Reference, Translator Execution, Limitations, License, Credits, Related Projects) unchanged.
  </action>
  <verify>
    <automated>grep -c "arxiv.org/abs/2303.08774" packages/node/README.md | grep -q "[1-2]" && grep -c "XPath 1.0" packages/node/README.md | grep -q "[1-2]" && grep -c "preprint" packages/node/README.md | grep -q "1" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - packages/node/README.md contains `https://arxiv.org/abs/2303.08774` in quick-start
    - Output shows `itemType`, `title`, `creators`, `date`, `DOI` fields with realistic values
    - XPath caveat present: "XPath support uses an xmldom bridge (XPath 1.0). XPath 2.0 is not supported."
    - No NYTimes example URL remains in the file
  </acceptance_criteria>
  <done>Node README quick-start uses arXiv URL with realistic output and includes XPath caveat</done>
</task>

<task type="auto">
  <name>Task 3: Bump ztractor-node to 1.0.0 and fix workspace dependency</name>
  <files>packages/node/package.json</files>
  <read_first>
    - packages/node/package.json
    - packages/core/package.json
  </read_first>
  <action>
In packages/node/package.json, make exactly two changes:

1. Change `"version": "0.1.0"` to `"version": "1.0.0"`
2. Change `"ztractor": "workspace:*"` to `"ztractor": "^1.0.0"` in the dependencies section

The workspace:* protocol is a Bun/pnpm workspace feature that won't resolve on npm. It must be a real semver range before publishing. Use ^1.0.0 to match the core package version.

Do NOT change any other fields.
  </action>
  <verify>
    <automated>grep '"version": "1.0.0"' packages/node/package.json && grep '"ztractor": "\^1.0.0"' packages/node/package.json && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - packages/node/package.json has `"version": "1.0.0"`
    - packages/node/package.json has `"ztractor": "^1.0.0"` (not workspace:*)
    - All other fields in package.json are unchanged
  </acceptance_criteria>
  <done>ztractor-node version is 1.0.0 and depends on ztractor ^1.0.0</done>
</task>

</tasks>

<verification>
- Both READMEs reference arXiv URL: `grep -l "arxiv.org/abs/2303.08774" packages/*/README.md` returns 2 files
- No NYTimes URLs remain: `grep -r "nytimes" packages/*/README.md` returns nothing
- Node package version is 1.0.0: `grep '"version": "1.0.0"' packages/node/package.json`
- Workspace dependency replaced: `grep -c "workspace" packages/node/package.json` returns 0
</verification>

<success_criteria>
Both READMEs have arXiv quick-start examples with realistic output. ztractor-node is version 1.0.0 with real semver dependency on ztractor ^1.0.0. No placeholder or fake URLs remain.
</success_criteria>

<output>
After completion, create `.planning/phases/06-publish/06-01-SUMMARY.md`
</output>
