{
    "name": "ztractor",
    "version": "0.1.0",
    "description": "Core browser-compatible logic for Ztractor",
    "type": "module",
    "main": "dist/index.js",
    "module": "dist/index.js",
    "types": "dist/index.d.ts",
    "files": [
        "dist"
    ],
    "scripts": {
        "build": "bun bundle-translators.ts && bun build src/index.ts --outdir dist --target browser --format esm",
        "test": "bun test",
        "postinstall": "bun bundle-translators.ts"
    },
    "devDependencies": {
        "typescript": "^5",
        "@types/bun": "^1.3.2"
    }
}
